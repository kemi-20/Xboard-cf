{
  "name": "xboard-edge",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.mjs",
    "deploy": "wrangler deploy --experimental-provision --keep-vars"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.53.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "sql.js": "^1.13.0",
    "yaml": "^2.8.1"
  }
}
